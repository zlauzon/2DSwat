extends Particles2D


var heightMax = 1.5
var heightMin = 0.5


# Called when the node enters the scene tree for the first time.
func _ready():
	emitting = true


# Called every frame. 'delta' is the elapsed time since the previous frame.
#func _process(delta):
	#print(get_speed_scale())
#	if ($Lifetime.time_left == 28):
#		pass
	#	process_material.set_param(process_material.PARAM_INITIAL_LINEAR_VELOCITY, 0)
	#	print(process_material.initial_velocity)
